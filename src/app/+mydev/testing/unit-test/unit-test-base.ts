export function solveExpression(x) {
  if (x < 0) {
    return 0;
  }

  return x + 1;
}

export function getString(text) {
  return `Hello unit ${text}`;
}

export function getArray() {
  return ['one', 'two', 'three'];
}


